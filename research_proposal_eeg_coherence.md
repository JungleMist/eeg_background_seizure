# 研究提案

## 基于物理点源模型的 EEG 特异性成分提取：在浅层 CNN 框架下的癫痫背景脑电分类可行性研究

---

**数据集**：TUH EEG Epilepsy Corpus (TUEP v3.1.0)  
**任务**：基于背景 EEG 的癫痫患者 vs. 正常被试二分类

---

## 目录

1. [研究背景与动机](#1-研究背景与动机)
2. [核心研究假设](#2-核心研究假设)
3. [研究问题](#3-研究问题)
4. [数据集与预处理](#4-数据集与预处理)
5. [核心方法论：物理点源 Wiener 分解](#5-核心方法论物理点源-wiener-分解)
6. [对照方法](#6-对照方法)
7. [实验设计](#7-实验设计)
8. [模型架构](#8-模型架构)
9. [评估协议](#9-评估协议)
10. [预期结果](#10-预期结果)
11. [研究边界与局限性](#11-研究边界与局限性)
12. [论文结构](#12-论文结构)

---

## 1. 研究背景与动机

### 1.1 临床背景

癫痫影响全球约 1% 的人口，其早期诊断和长期管理高度依赖头皮脑电图（EEG）。绝大多数 EEG 机器学习研究聚焦于**发作期（ictal）信号**的检测与分类，而对**发作间期背景 EEG（interictal background EEG）** 的系统研究相对匮乏。

发作间期背景 EEG 包含 trait-level 的癫痫网络特征——即使在无临床发作的状态下，癫痫患者的皮层局部活动模式与正常被试之间存在系统性差异。这种差异是否可以被可靠提取，是本研究的出发点。

### 1.2 方法论空白

现有 EEG 深度学习研究的两种主流范式存在以下问题：

| 范式 | 问题 |
|---|---|
| 直接输入原始信号至深度模型 | 信号中混有容积传导、参考电极共享等非神经源噪声，模型可能学到系统性伪迹 |
| 传统手工特征（PSD、相干性等）| 未对信号成分进行显式分离，判别信息与干扰混杂 |

目前**尚无**针对背景 EEG 特性、基于物理传导模型设计的显式特征工程方法，并在深度学习框架下进行系统评估。

### 1.3 研究定位

本研究提出一种基于**颅内点源物理传导模型**的信号分解方法，作为 EEG 预处理的特征工程步骤，并在以下三类模型框架下评估其有效性：传统机器学习（SVM、XGBoost）和浅层 CNN。

---

## 2. 核心研究假设

### 2.1 假设表述

> **在背景 EEG 的语境下，导联间的相干成分（共有频率分量）主要来源于非神经性的物理干扰（容积传导、参考电极污染、全局慢波节律等），其物理来源可等效为颅内若干点源。各导联观测到的相干成分，是该点源信号经过频率相关幅度衰减和相位偏移后的叠加。导联各自的独立成分（特异性成分）才真正反映局部皮层的活动状态，包含癫痫 trait-level 特征。**

### 2.2 与主流观点的分歧

主流功能连接研究认为导联间相干性 = 脑区间真实神经耦合，因此相干性本身是有意义的特征。

本研究在**背景 EEG**语境下提出相反立场：

```
主流观点（发作期为主）：
    相干性 → 痫性放电同步扩散 → 有意义的神经耦合

本研究假设（背景 EEG）：
    相干性 → 容积传导 + 参考污染 + 全局节律 → 干扰
    特异性成分 → 局部皮层活动 → 真实判别信息
```

### 2.3 假设的合理性基础

- **背景 EEG 研究的文献匮乏**：相关假设尚未被系统检验，不存在充分的反证
- **生物学动机**：癫痫患者局部皮层活动（兴奋/抑制失衡）的特异性改变是已知病理机制
- **类比 EKG 去除**：EKG 信号作为全局同步干扰的去除是成熟实践，本方法是其在宽带 EEG 中的频率相关推广
- **数学可验证性**：分解后的特异性成分与其他导联的相干度理论上趋近于零，可直接验证

---

## 3. 研究问题

**核心问题 1**：基于物理点源模型设计的显式特征工程（Wiener 分解提取特异性成分），在浅层 CNN 框架下是否有效提升癫痫背景 EEG 的分类性能？

**核心问题 2**：基于物理点源模型设计的显式特征工程（Wiener 分解提取特异性成分），在 XGBoost 框架下训练是否有效？特异性成分所包含的手工特征，是否构成可被树模型利用的有效分类信息？

**子问题**：

- **Sub-Q1**：特异性成分提取对 CNN 的分类增益，是否优于无特征工程的基线？
- **Sub-Q2**：CNN 能否自主学习到与特异性成分提取等效的信息？（通过 CAM/SHAP 验证）
- **Sub-Q3**：频率相关 Wiener 分解，相比固定标量补偿（豆包方案等价实现），是否带来可量化的优势？
- **Sub-Q4**：物理点源假设是否在数据中得到支持？（通过传递性约束验证）
- **Sub-Q5**：在 XGBoost 框架下，基于 SHAP 的横向比较（Wiener 特异性成分 vs ICA vs 原始信号）中，三种预处理条件下模型所关注的特征重心是否发生系统性偏移？哪些频带、哪些导联对的特征在特异性成分条件下贡献度显著变化？
- **Sub-Q6**：Wiener 特异性成分所携带的分类信息是否独立有效？通过 XGBoost-SHAP 评估特异性成分特征的判别力，验证特异性成分确实保留了（而非损失了）癫痫 trait-level 信息。

---

## 4. 数据集与预处理

### 4.1 TUEP v3.1.0 概述

| 属性 | 值 |
|---|---|
| 来源 | Temple University Hospital EEG Corpus |
| 被试构成 | 100 名癫痫患者 + 100 名正常对照 |
| 类别比 | 1:1（完全平衡） |
| 导联类型 | **单极导联**（AR：平均参考 / LE：链耳参考） |
| 标注内容 | 背景段（bckg）、发作事件、元数据（诊断、药物） |
| 关键特性 | 单极导联满足 ICA/Wiener 分解的矩阵满秩前提 |

> **注**：TUEP 使用单极导联而非 TCP 双极导联，这一特性使 ICA 和 Wiener 分解在方法论上完全合法，是选用此数据集的关键原因之一。

### 4.2 参考方案处理

**阶段一（主实验）**：手动筛选，保证所有样本使用**同一参考方案**（AR 或 LE，取数量较多者），消除参考差异对模型的混淆。

**阶段二（扩展实验）**：混合 AR+LE 数据集，验证特异性成分提取方法对参考方案的**鲁棒性**（理论上，去除共有成分后参考差异的影响减弱）。

### 4.3 背景段提取

```
Step 1  读取 .csv/.csv_bi 标注文件
Step 2  保留 label = 'bckg' 的时间段
Step 3  剔除发作事件前后各 30 秒的缓冲区（避免预/后发作期污染）
Step 4  按 8 秒长度切割 epoch（与 TUEG 注释工具默认视窗一致）
Step 5  应用幅值阈值（±200 μV）剔除明显伪迹 epoch
```

### 4.4 数据划分

按 **Subject ID** 严格划分，防止同一被试的不同 epoch 跨集出现（数据泄露）：

```
训练集：140 人（70%）  → ~2800 epoch（估计）
验证集： 20 人（10%）
测试集： 40 人（20%）
```

---

## 5. 核心方法论：物理点源 Wiener 分解

### 5.1 物理模型

设颅内存在一个（或若干）干扰点源，其信号为 $s(t)$。该源到达第 $i$ 个电极时，经历**频率相关**的复值传输：

$$X_i(f) = G_i(f) \cdot S(f) + \Xi_i(f)$$

其中：
- $G_i(f) = \alpha_i(f) \cdot e^{j\phi_i(f)}$：**复值传输函数**，同时包含频率相关幅度衰减和相位偏移
- $\Xi_i(f)$：导联 $i$ 的特异性成分（目标保留量）
- 假设 $\Xi_i(f)$ 与 $S(f)$ 不相关

**与豆包方案（EKG 类比）的关系**：

豆包方案等价于令 $G_i(f) = c_i$（固定复数标量），即幅度比和相位差不随频率变化。这对窄带干扰（EKG ~1Hz）完全成立，但对宽带 EEG（0.5–40 Hz）需要推广为频率相关形式。两者处于同一框架，豆包方案是特殊情形。

### 5.2 相对传输函数的估计

对于导联组内任意一对 $(x_i, x_j)$，相对传输函数：

$$T_{ij}(f) = \frac{G_i(f)}{G_j(f)}$$

通过 Wiener 滤波器从互功率谱密度进行**数据驱动估计**：

$$\hat{T}_{ij}(f) = \frac{S_{ij}(f)}{S_{jj}(f)} = \frac{\mathbb{E}[X_i(f) \cdot X_j^*(f)]}{\mathbb{E}[|X_j(f)|^2]}$$

**估计的正确性**（代入观测模型，高信噪比近似）：

$$\hat{T}_{ij}(f) \approx \frac{G_i(f) \cdot G_j^*(f) \cdot P_s(f)}{|G_j(f)|^2 \cdot P_s(f)} = \frac{G_i(f)}{G_j(f)} = T_{ij}(f) \checkmark$$

**物理量解读**：

```
幅度比  |T̂_ij(f)|：
    → 若近似常数（不随 f 变化）：纯几何衰减，与豆包方案一致
    → 若随 f 单调下降：高频被组织选择性吸收
    → 提供频率相关的组织传导特性信息

相位差  ∠T̂_ij(f)：
    → 若 ∝ f（线性）：等效为纯传播时延
    → 若非线性：组织的频率色散效应
    → 生理合理范围：EEG 频段内通常 < π/4
```

### 5.3 多导联向量 Wiener 滤波器

对解剖组 $\mathcal{G} = \{x_1, \ldots, x_k\}$（例如额叶组或双侧对称组），对目标导联 $x_i$ 的干扰估计利用**组内所有其他导联**的联合信息：

$$x_i^{\text{coherent}}(f) = \mathbf{h}_i^\top(f) \cdot \mathbf{X}_{-i}(f)$$

向量 Wiener 滤波器：

$$\mathbf{h}_i(f) = \mathbf{S}_{-i,-i}^{-1}(f) \cdot \mathbf{s}_{i,-i}(f)$$

其中 $\mathbf{S}_{-i,-i}(f)$ 为参考集合的 $(k-1)\times(k-1)$ 交叉功率谱矩阵，$\mathbf{s}_{i,-i}(f)$ 为目标导联与参考集的互功率谱向量。

**相比配对处理的优势**：

```
配对处理：x_i^specific = x_i - T̂_ij · x_j
    → 仅消除 x_j 能解释的干扰
    → 若 x_j 自身含特异性成分，会引入"污染"

向量处理：x_i^specific = x_i - Σ_j h_ij(f)·x_j
    → 利用组内所有导联的联合信息，估计更精确
    → 对参考导联中的特异性成分具有内在抑制
```

### 5.4 导联分组策略

采用**运动伪迹传导路径分组**，以颈部及面部肌肉运动的解剖传导路径作为分组依据，而非简单的双侧同名对称：

```
组 G1  FP1–FP2
       运动类型：面部对称运动（皱眉、挤眼，最常见）
       传导：额肌/眼轮匝肌伪迹在额极双侧等幅出现

组 G2  F7–T3
       运动类型：向左转颈或向左低头（胸锁乳突肌，左侧）
       传导：SCM 附着点（下颌后方 F7 区域）→ 颞区（T3）沿皮肤传导衰减

组 G3  T3–T5–O1         ← 三导联组
       运动类型：仰头运动（斜方肌 / 头夹肌，左侧）
       传导：颈部后外侧肌群（T3）→ 后颞（T5）→ 枕（O1）的传导梯度

组 G4  O1–O2
       运动类型：任何颈部活动引起的枕部双侧对称分量
       传导：枕肌在双侧枕极等幅出现；仰头、低头均可激活

对侧镜像（左右对称运动的对应组）：

组 G5  F8–T4             ← G2 的右侧镜像
       运动类型：向右转颈或向右低头（胸锁乳突肌，右侧）

组 G6  T4–T6–O2         ← G3 的右侧镜像（三导联组）
       运动类型：仰头运动（斜方肌 / 头夹肌，右侧）
```

**未参与分解的导联**：F3, F4, F7\*, F8\*, C3, C4, P3, P4, Fz, Cz, Pz — 这些位置的运动伪迹传导路径在上述六组中未被覆盖，保留原始信号。  
（\* F7/F8 已分别归入 G2/G5，此处指其他额顶区导联）

**与解剖对称对方案的对比**：

| 维度 | 旧方案（双侧对称对） | 新方案（运动传导路径组） |
|---|---|---|
| 物理动机 | 容积传导的左右对称性 | 颈/面部肌肉的实际解剖传导路径 |
| 组结构 | 全部为 2 导联对 | 2 导联对（G1/G2/G4/G5）+ 3 导联链（G3/G6） |
| 覆盖范围 | 8 对，16 导联 + 3 中线 | 6 组，13 导联参与分解 |
| 3 导联组优势 | 不支持 | G3/G6 利用三点梯度，向量 Wiener 可联合估计两段传导比 |
| 可解释性 | 依赖半球间不对称假设 | 直接对应临床可观察的头颈运动类型 |

**选择运动传导路径分组的理由**：
1. **伪迹来源的物理先验更精确**：背景 EEG 的主要低频干扰来自颈肌/面肌运动，其传导方向沿皮肤表面从肌肉附着点向远端衰减，恰好对应上述路径链
2. **三导联组提供额外约束**：G3/G6 中 T3-T5 和 T5-O1 两段的相对传输函数须满足传递性 $T_{T3,O1} = T_{T3,T5} \cdot T_{T5,O1}$，可作为单点源假设的强验证条件
3. **对称镜像对（G2↔G5，G3↔G6）提供左右差异信息**：相同运动在两侧产生的传导函数应相近，若癫痫患者中出现系统性左右不对称，是额外的判别信号

### 5.5 完整分解流程

```
输入：运动传导路径组 G = {x_1, ..., x_k}（k=2 或 k=3），分析频带 [0.5, 40] Hz

Step 1  互功率谱估计
        使用 Welch 法，nperseg = 1000（8秒@125Hz），
        频率分辨率 = 0.5 Hz
        输出：复值矩阵 S(f)，形状 (k, k, n_freqs)

Step 2  相干度阈值检查
        计算最大成对相干度 max_{i≠j} Coh_ij(f)
        若目标频带内相干度 < θ（默认 θ = 0.15），
        则跳过该组分解（干扰功率低，分解无意义，保留原始信号）

Step 3  向量 Wiener 滤波器构造
        对组内每个导联 x_i：
            h_i(f) = S_{-i,-i}^{-1}(f) · s_{i,-i}(f)
        记录 |h_i(f)| 和 ∠h_i(f) 供物理验证使用

Step 4  频域重构干扰成分
        X_i^coherent(f) = h_i^T(f) · X_{-i}(f)
        x_i^coherent(t) = IFFT[X_i^coherent(f)]

Step 5  提取特异性成分
        x_i^specific(t) = x_i(t) - x_i^coherent(t)

Step 6  物理一致性验证（Sanity Check，每被试每组记录）
        验证 ①：Coh(x_i^specific, x_j) → 0（分解纯净性）
        验证 ②：|h_ij| · |h_jk| ≈ |h_ik|（幅度传递性）
        验证 ③：∠h_ij + ∠h_jk ≈ ∠h_ik（相位传递性）
        → 传递性成立：支持单点源假设
        → 传递性不成立：提示多干扰源，记录到讨论
```

### 5.6 固定标量模式（消融对照）

为量化频率相关性的贡献，同时实现**固定标量补偿模式**（对应豆包方案）：

$$\hat{c}_{ij} = \frac{1}{f_u - f_l}\int_{f_l}^{f_u} \hat{T}_{ij}(f)\, df$$

对两种模式分别运行分解，比较残余相干度，直接量化频率相关建模的增益。

---

## 6. 对照方法

### 条件 B：ICA 特征工程

```
原始单极信号
    → FastICA 分解（n_components = n_channels）
    → 自动识别伪迹成分（相关性准则：与 EOG/EMG 代理信号的相关性）
    → 去除伪迹成分，保留脑源成分
    → 重构信号
    → 特征提取 / CNN 输入
```

**与新方法的对比定位**：ICA 假设所有源统计独立，是通用盲源分离方法，对背景 EEG 无针对性假设；新方法专门基于宽带背景 EEG 的干扰物理特性设计。

### 条件 C：无特征工程（基线）

原始单极信号直接进入特征提取或 CNN，不做任何预处理（仅标准化）。

用于量化任何特征工程的增益下限。

---

## 7. 实验设计

### 7.1 完整实验矩阵（3 × 3）

|  | **SVM** | **XGBoost** | **浅层 CNN** |
|---|---|---|---|
| **C：无特征工程（基线）** | C-SVM | C-XGB | C-CNN |
| **B：ICA** | B-SVM | B-XGB | B-CNN |
| **A：Wiener 分解（新方法）** | A-SVM | A-XGB | **A-CNN ★** |

★ **A-CNN 是研究的主要贡献**，其余格子提供完整的对照体系。

### 7.2 矩阵的三维读法

```
纵向比较（固定模型）：
    C → B → A 的性能变化 = 特征工程对该模型的增益 ΔFeature

横向比较（固定特征工程）：
    SVM → XGBoost → CNN 的性能变化 = 模型能力对特征工程依赖程度 ΔModel

对角线比较（核心命题）：
    A-CNN vs C-SVM：新方法+轻量CNN 是否可以接近或超过 无工程+传统模型
```

### 7.3 消融实验

| 消融条件 | 目的 |
|---|---|
| **A-freq**：频率相关 Wiener（完整方法） | 基准 |
| **A-scalar**：固定标量补偿（豆包方案等价） | 量化频率相关性的贡献 |
| **A-coherence-only**：相干矩阵展平特征（不做重参考） | 验证分解 vs 直接用相干性作特征 |
| **A-combined**：特异性成分特征 + 相干矩阵特征 | 是否互补 |

### 7.4 物理假设验证实验

独立于分类任务，专门验证物理模型假设：

```
实验 V1：分解纯净性
    比较所有导联对在分解前后的成对相干度
    预期：x_i^specific 与 x_j 的相干度显著低于 x_i 与 x_j

实验 V2：传递性约束（单点源检验）
    计算所有三角形导联组合 (i, j, k) 的传递性误差：
        ε_amplitude = ||h_ij| · |h_jk| - |h_ik||
        ε_phase     = |∠h_ij + ∠h_jk - ∠h_ik|
    分别在癫痫组和正常组中报告 ε 的分布
    → 是否支持单点源假设

实验 V3：频率相关性量化
    对比 |h_ij(f)| 在各频带的方差
    → 若方差大，支持频率相关模型优于固定标量
```

---

## 8. 模型架构

### 8.1 浅层 CNN（主要深度学习模型）

设计原则：参数量控制在 ~18K，适配约 2800 训练样本规模。

```python
class ShallowEEGCNN(nn.Module):
    """
    输入：(Batch, N_channels, T)
         例如 (B, 19, 1000) → 19 通道，8秒 @125Hz

    架构参考 EEGNet 的 Depthwise+Pointwise 分离设计，进一步轻量化。
    """
    def __init__(self, n_channels=19, n_classes=2):
        super().__init__()

        # Stage 1：时间局部特征（Depthwise，通道间不混合）
        self.temporal = nn.Sequential(
            nn.Conv1d(n_channels, n_channels*4,
                      kernel_size=25, padding=12, groups=n_channels),
            nn.BatchNorm1d(n_channels*4),
            nn.ELU(),
            nn.AvgPool1d(4),          # 时间降采样 4×
            nn.Dropout(0.3)
        )

        # Stage 2：跨通道空间融合（Pointwise）
        self.spatial = nn.Sequential(
            nn.Conv1d(n_channels*4, 64, kernel_size=1),
            nn.BatchNorm1d(64),
            nn.ELU(),
            nn.Dropout(0.3)
        )

        # Stage 3：更大时间尺度模式
        self.temporal2 = nn.Sequential(
            nn.Conv1d(64, 64, kernel_size=11, padding=5),
            nn.BatchNorm1d(64),
            nn.ELU(),
            nn.AdaptiveAvgPool1d(1),  # 全局时间聚合
            nn.Dropout(0.4)
        )

        self.classifier = nn.Linear(64, n_classes)

    def forward(self, x):
        x = self.temporal(x)     # (B, C*4, T/4)
        x = self.spatial(x)      # (B, 64, T/4)
        x = self.temporal2(x)    # (B, 64, 1)
        return self.classifier(x.squeeze(-1))
```

**输入一致性原则**：三种特征工程条件（A/B/C）的 CNN 输入格式完全相同——`(B, 19, 1000)`——唯一的变量是信号内容（原始 / ICA重构 / Wiener特异性成分），保证对照公平性。

### 8.2 SVM

- 核函数：RBF（径向基函数）
- 正则化：L2（参数 C）
- 超参搜索：Grid Search，`C ∈ {0.1, 1, 10, 100}`，`γ ∈ {scale, auto, 0.01, 0.001}`
- 输入：标准化手工特征向量
- 手工特征：各频带相对功率、Hjorth 参数、谱熵（在相应预处理信号上计算）

### 8.3 XGBoost

- 正则化：L1 + L2（alpha, lambda）
- 超参：`max_depth ∈ {3,4,5,6}`，`learning_rate ∈ {0.05, 0.1, 0.3}`，`n_estimators ∈ {100, 200, 500}`
- 特征重要性：SHAP 值分析
- 输入：与 SVM 相同的手工特征向量

### 8.4 CNN 训练策略

```
优化器：AdamW，lr = 1e-3，weight_decay = 1e-4
学习率调度：CosineAnnealingLR（T_max = 100 epochs）
早停：patience = 20，监控验证集 AUROC
损失函数：CrossEntropyLoss（类别 1:1，无需加权）
Batch Size：32
数据增强（仅训练集）：
    - 时间轴随机裁剪（保留 80-100% 长度，零填充至 1000）
    - 加性高斯噪声（σ = 0.01 × 信号标准差）
正则化：Dropout 0.3 / 0.4（见架构定义）
```

### 8.5 被试级别推理聚合

```
训练：以 epoch 为单位（单个 8 秒片段）
推理：
    1. 同一被试的 N 个 epoch 各自独立通过模型
    2. 对 N 个 softmax 输出取均值 → 被试级别概率
    3. 最终分类性能在被试级别评估

意义：减少单 epoch 的随机方差，提高被试级别预测稳定性
```

---

## 9. 评估协议

### 9.1 主要指标

| 指标 | 选用理由 |
|---|---|
| **AUROC** | 主要指标；对类别平衡（1:1）稳定；阈值无关 |
| **F1-Score (macro)** | 综合精度与召回；类别平衡时与 Accuracy 趋同 |
| **Accuracy** | 直观可读；类别平衡时可靠 |
| **SHAP 值** | 特征级别可解释性；用于分析哪些导联对、哪些频带最具判别力 |

### 9.2 三维比较框架

```
纵向（ΔFeature）：
    固定模型，比较 C → B → A
    量化公式：ΔFeature_A = AUROC(A-Model) - AUROC(C-Model)

横向（ΔModel）：
    固定特征工程，比较 SVM → XGBoost → CNN
    量化公式：ΔModel_CNN = AUROC(A-CNN) - AUROC(A-SVM)

交互效应：
    检验 ΔFeature 在不同模型间是否一致
    → 若一致：特征工程效果独立于模型容量
    → 若 CNN 下 ΔFeature 较小：CNN 部分自主学到了相关信息
```

### 9.3 可解释性分析

```
XGBoost：SHAP Summary Plot
    → 哪些频带特征对分类贡献最大？
    → 在 A 条件下，哪些导联对的特异性成分最具判别力？

CNN：Gradient-weighted Class Activation Map（Grad-CAM）
    → CNN 关注的时间位置和通道与特异性成分提取的导联组是否一致？
    → 若一致：支持物理模型假设；若不一致：提示 CNN 学到了不同信息
```

---

## 10. 预期结果

### 10.1 核心研究假设的预测结论

**H1（强假设）**：`A-CNN > C-CNN`

> 物理点源 Wiener 分解对 CNN 具有正向增益。显式去除共有干扰成分后，各导联的局部特异性活动更加突出，CNN 能够更容易学到癫痫相关的 trait-level 模式。预计 AUROC 提升 0.03–0.08。

**H2**：`A-CNN ≥ A-SVM`

> CNN 在特异性成分信号上的学习能力不弱于传统模型。轻量 CNN（~18K 参数）在约 2800 训练样本上，配合 Dropout 和数据增强，可达到与 SVM 相当或更好的性能。

**H3（差异化假设）**：`A-CNN > B-CNN`

> 针对背景 EEG 物理特性专门设计的 Wiener 分解，优于通用盲源分离方法（ICA）。ICA 假设统计独立性，不针对特定物理传导模型；Wiener 分解直接建模幅度和相位的频率相关传导，去除更精确。

**H4（消融假设）**：`A-freq > A-scalar`

> 频率相关 Wiener 滤波的残余相干度低于固定标量补偿，在下游分类性能上体现出可量化差异，支持宽带 EEG 需要频率相关建模的论点。

### 10.2 物理验证实验预期结果

| 实验 | 预期结果 | 意义 |
|---|---|---|
| V1：分解纯净性 | 分解后相干度从 ~0.4 降至 ~0.05 以下 | 证明分解有效去除了共有成分 |
| V2：传递性约束 | ε < 0.1（幅度），ε < π/8（相位），正常组优于癫痫组 | 支持单点源假设；癫痫组传递性偏差更大，提示多源或网络同步 |
| V3：频率相关性 | `|h_ij(f)|` 在 delta→beta 间变化幅度 > 20% | 支持频率相关模型的必要性 |

### 10.3 次级发现（如 H1 不成立时的价值）

若 `A-CNN ≤ C-CNN`（即特征工程对 CNN 无增益）：

```
这本身是重要的负结果，具有以下学术价值：
1. 通过 Grad-CAM 分析，验证 CNN 是否已自主学到特异性成分模式
2. 若是：说明 CNN 的归纳偏置足以发现物理传导结构，
         为设计针对 EEG 物理特性的 CNN 结构提供洞见
3. 若否：说明背景 EEG 的 trait-level 特征过于微弱，
         任何预处理均不足以在有限样本下提升 CNN 性能，
         指向需要预训练模型（如 LaBraM 微调）的研究方向
```

---

## 11. 研究边界与局限性

### 11.1 假设的内在局限

| 局限性 | 描述 | 处理策略 |
|---|---|---|
| 多点源问题 | 真实颅内干扰可能来自多个点源，单点源模型是简化 | 传递性验证实验定量评估（G3/G6 三导联组提供额外三角形约束）；论文 Limitation 章节讨论 |
| 真实神经同步被误去除 | 癫痫患者的广泛同步放电（GPD等）在背景段仍可能存在，其半球间相干属真实神经耦合 | 严格的背景段筛选（30秒缓冲）+ 消融实验 V1/V2 |
| 低 SNR 被试 | 若特异性成分功率远低于共有成分，Wiener 估计退化 | 相干度阈值 θ = 0.15，低于阈值不触发分解 |
| 导联覆盖不全 | F3/F4/C3/C4/P3/P4/Fz/Cz/Pz 不在任何运动传导路径组内，保留原始信号；顶叶区域的伪迹路径未被建模 | 可后续扩展以额-顶功能区块或数据驱动分组作为延伸；当前方案覆盖临床伪迹的高发区域（额颞枕链） |
| 左右非对称运动 | 若头颈运动同时激活双侧（如前倾），G2↔G5 可能同时响应，分解假设弱化 | O1-O2（G4）专门处理双侧枕部共有分量；G3/G6 中仍可从单侧梯度估计传导 |

### 11.2 数据层面局限

- 样本量（200 被试）对 CNN 构成约束，限制更复杂架构的探索
- TUEP 均为 Temple University Hospital 临床样本，泛化性需在其他机构数据集上验证
- 手动筛选单一参考方案可能进一步减少可用被试数量（依赖实际数据分布）

### 11.3 方法论边界

- 本研究的特征工程针对单极导联设计，不适用于 TCP 双极导联（数学上差分运算已消去参考）
- Wiener 滤波的频率分辨率（0.5 Hz）受限于 epoch 长度（8 秒），极低频成分（< 1 Hz）估计不稳定

---

## 12. 论文结构

```
1. Introduction
   1.1 背景 EEG 的癫痫诊断价值与研究现状
   1.2 头皮 EEG 信号中共有成分的物理来源
   1.3 特征工程与深度学习的交互：未被系统研究的问题
   1.4 研究贡献声明

2. Related Work
   2.1 发作间期 EEG 分类综述
   2.2 EEG 参考方案与表面拉普拉斯方法
   2.3 ICA 在 EEG 中的应用
   2.4 EEG 深度学习：EEGNet 及其变体
   2.5 相关数据集：TUEP、TUSZ、TUAB

3. Methods
   3.1 数据集（TUEP v3.1.0，筛选与预处理策略）
   3.2 物理点源模型与核心假设
   3.3 向量 Wiener 分解：数学推导与实现
       3.3.1 传输函数的数据驱动估计
       3.3.2 导联分组策略（运动伪迹传导路径：G1–G6）
       3.3.3 频率相关模式 vs 固定标量模式
       3.3.4 完整分解流程与物理验证
   3.4 对照特征工程（ICA、无工程基线）
   3.5 模型架构（SVM、XGBoost、浅层 CNN）
   3.6 训练与推理策略

4. Experiments
   4.1 实验矩阵设计（3×3）与分析框架
   4.2 评估协议（被试级别 AUROC/F1 + SHAP）
   4.3 消融实验设计

5. Results
   5.1 物理假设验证（分解纯净性、传递性、频率相关性）
   5.2 整体分类性能（3×3 矩阵）
   5.3 纵向分析：特征工程对各模型的增益 ΔFeature
   5.4 横向分析：CNN vs 传统模型在各特征工程下的表现 ΔModel
   5.5 消融实验：频率相关 vs 固定标量

6. Discussion
   6.1 物理点源模型的适用性与局限
   6.2 CNN 能否自主学习特异性成分信息（Grad-CAM 分析）
   6.3 与 ICA 的本质差异及场景互补性
   6.4 局限性与未来工作
       - 多点源扩展
       - 预训练模型（LaBraM 等）作为下游分类器
       - 在 TUSZ 背景段上的扩展验证

7. Conclusion
```

---

## 附录：核心公式速查

| 符号 | 含义 |
|---|---|
| $X_i(f)$ | 导联 $i$ 的频域表示 |
| $G_i(f) = \alpha_i(f) \cdot e^{j\phi_i(f)}$ | 点源到导联 $i$ 的复值传输函数 |
| $T_{ij}(f) = G_i(f) / G_j(f)$ | 导联对的相对传输函数 |
| $\hat{T}_{ij}(f) = S_{ij}(f) / S_{jj}(f)$ | Wiener 估计量（互功率谱 / 自功率谱） |
| $\mathbf{h}_i(f) = \mathbf{S}_{-i,-i}^{-1}(f) \cdot \mathbf{s}_{i,-i}(f)$ | 向量 Wiener 滤波器 |
| $x_i^{\text{specific}}(t) = x_i(t) - \text{IFFT}[\mathbf{h}_i^\top(f) \cdot \mathbf{X}_{-i}(f)]$ | 特异性成分提取 |
| $\text{Coh}_{ij}(f) = \|S_{ij}(f)\|^2 / [S_{ii}(f) \cdot S_{jj}(f)]$ | 相干度（验证分解纯净性） |

---

*文档版本：v1.1 · 2026年5月21日*  
*v1.1 变更：第 5.4 节导联分组策略由"双侧解剖对称对"修订为"运动伪迹传导路径组"（G1–G6），含两个三导联链（G3: T3–T5–O1，G6: T4–T6–O2）；同步更新第 11.1 节局限性分析及论文结构 3.3.2 节。*  
*基于与 Claude Sonnet 4.6 的完整研究讨论整理*
