# 研究提案

## 面向癫痫背景 EEG 的物理约束预处理算法研究：基于相干成分分解的伪迹抑制与神经信号保真度评估

---

- **数据集**：TUH EEG Epilepsy Corpus (TUEP v3.1.0)
- **任务场景**：基于发作间期背景 EEG 的癫痫患者 vs. 正常对照二分类
- **研究重点**：数据预处理算法，而非分类模型本身
- **当前验证工具**：XGBoost + 211 维手工特征 + SHAP 归因分析

---

## 目录

1. [研究背景与动机](#1-研究背景与动机)
2. [当前研究定位](#2-当前研究定位)
3. [核心假设](#3-核心假设)
4. [研究问题](#4-研究问题)
5. [数据集与基础预处理](#5-数据集与基础预处理)
6. [核心方法：物理约束 Wiener 预处理方法族](#6-核心方法物理约束-wiener-预处理方法族)
7. [对照方法与消融条件](#7-对照方法与消融条件)
8. [实验设计与评估协议](#8-实验设计与评估协议)
9. [当前实验观察与解释框架](#9-当前实验观察与解释框架)
10. [研究边界与局限性](#10-研究边界与局限性)
11. [预期贡献](#11-预期贡献)
12. [论文结构建议](#12-论文结构建议)

---

## 1. 研究背景与动机

### 1.1 临床背景

癫痫的诊断和长期管理高度依赖头皮脑电图（EEG）。相较于发作期信号，发作间期背景 EEG 更接近患者的长期神经网络状态，可能包含 trait-level 的癫痫相关信息，例如背景节律组织性改变、局灶性慢波、颞区节律异常、半球间不对称等。

然而，背景 EEG 的判别信号通常比发作期放电更弱，也更容易被非神经源成分掩盖。头皮 EEG 中的肌电、眼动、参考共享、容积传导和全局同步节律，会在多个导联之间形成相干成分。若直接把原始信号或逐通道统计特征输入分类模型，模型可能依赖这些稳定但非病理性的伪迹差异，而不是癫痫相关的神经活动。

### 1.2 方法论问题

当前研究的核心问题不是“哪个分类器分数最高”，而是：

> 如何设计一种具有明确物理含义的背景 EEG 预处理方法，使下游模型更少依赖伪迹，更可能利用具有神经生理意义的信号成分？

因此，本研究把机器学习流水线定位为预处理算法的验证工具。XGBoost 的 AUROC/F1/Accuracy 提供任务层面的量化反馈，SHAP 提供模型注意力迁移的解释线索，但它们并不是研究本身的主要贡献。

### 1.3 研究重心的调整

当前代码库和近期实验显示，研究重心已经转向预处理算法本身：

- Wiener 分解从单一 frequency 模式扩展为 frequency、scalar、phase-gated、zerophase 等方法族。
- XGBoost 流水线已经支持 raw、ICA、Wiener frequency、Wiener phase-gated、Wiener zerophase 的横向比较。
- 特征空间已经回退到稳定的 211 维手工特征，用于解释预处理条件之间的差异。
- CNN 流水线仍存在于代码库中，但近期没有继续围绕 CNN 推进实验设计或结果分析，因此在本提案中仅作为暂缓的未来验证方向。

---

## 2. 当前研究定位

### 2.1 研究对象

本研究面向 TUEP v3.1.0 中的发作间期背景 EEG。当前默认配置使用 AR 参考方案（`01_tcp_ar`），标准 19 导联，采样率统一到 125 Hz，频带限制在 0.5-40 Hz。

当前已归档的默认实验在 AR 参考下包含 178 名受试者：

| 划分 | 受试者数 | epoch 数 | 癫痫受试者 | 对照受试者 |
|---|---:|---:|---:|---:|
| train | 124 | 149224 | 67 | 57 |
| val | 17 | 5526 | 9 | 8 |
| test | 37 | 26346 | 20 | 17 |

该规模反映的是当前筛选、参考方案和缓存状态下的实际实验设置；后续实验应以归档目录中的 `data_stats.json` 为准。

### 2.2 研究对象不是分类器

下游分类器的作用是回答：

- 预处理后，模型是否仍能保留癫痫相关判别信息？
- 模型依赖的通道、频段和特征类型是否发生生理上可解释的迁移？
- 分类性能和 SHAP 分布之间是否出现“高分但伪迹驱动”或“分数一般但更生理可信”的张力？

因此，XGBoost 是当前主要验证框架；CNN 暂不作为主线。当前提案只保留 XGBoost/SHAP 与物理验证实验作为主要评估路径。

### 2.3 研究判断标准

本研究不把 AUROC 的单一提升视为预处理成功的唯一标准。更合理的判断包括：

1. **物理有效性**：预处理是否降低了导联间不希望保留的虚假相干？
2. **神经保真度**：预处理是否避免过度回归真实的同步神经活动？
3. **解释迁移**：模型注意力是否从明显伪迹导联迁移到更符合癫痫背景 EEG 生理学的区域或频段？
4. **下游可用性**：预处理后的信号是否仍保留足够的分类信息？

---

## 3. 核心假设

### 3.1 基础假设

背景 EEG 中，导联间相干成分包含大量非目标成分：肌源性伪迹沿头颈部传导路径扩散，眼动和额肌活动影响额极导联，参考共享和容积传导使多个导联呈现同步变化。这些成分可能与标签存在统计相关，但不一定反映癫痫的神经病理机制。

本研究假设：

> 若将每个导联信号分解为“组内可预测的相干成分”和“不可由组内邻近导联解释的特异性成分”，则特异性成分更可能暴露局部背景脑活动；相干成分则更多承载肌源性、参考性或传导性伪迹。

形式化地，对导联组内目标通道 $x_i(t)$：

$$x_i(t) = x_i^{\text{specific}}(t) + x_i^{\text{coherent}}(t)$$

其中：

$$x_i^{\text{coherent}}(f) = \mathbf{h}_i^\top(f)\mathbf{X}_{-i}(f)$$

$$x_i^{\text{specific}}(t) = x_i(t) - \mathcal{F}^{-1}\{x_i^{\text{coherent}}(f)\}$$

### 3.2 关键修正：不是所有相干都应被视为噪声

早期设想把导联间相干成分更强地视为干扰。但近期 Wiener zerophase 和 phase-gated 方向表明，这一假设需要更精细：

- 零相位、近同步、可由解剖传导路径解释的共享分量，更可能是容积传导或肌源性伪迹。
- 非零相位、具有传播延迟的共享分量，可能包含真实神经耦合，例如颞叶网络传播或背景节律组织性。
- 双侧同步的真实神经活动（例如枕区后头部优势节律）也可能表现为近零相位相干，因此不能简单等同于伪迹。

因此，当前研究问题已经从“去除所有相干成分是否有益”转向：

> 如何在伪迹去除力度和真实神经信号保真度之间找到可解释、可验证的平衡？

### 3.3 方法族视角

Wiener frequency、Wiener phase-gated、Wiener zerophase 和 scalar ablation 可以视为同一分解框架下的不同约束：

| 方法 | 约束 | 去除倾向 | 主要风险 |
|---|---|---|---|
| frequency | 复数逐频率滤波器 | 去除任意相位的组内可预测成分 | 可能误删真实神经耦合 |
| phase-gated | 复数滤波器 + 零参考相位门控 | 只去除相位接近 0 的共享成分 | 阈值过窄会残留伪迹 |
| zerophase | 实数逐频率滤波器 + 相位门控 | 更保守地去除零相位共享成分 | 伪迹去除召回率较低 |
| scalar | 频带平均固定标量 | 近似传统固定补偿 | 无法表达频率相关传导 |

---

## 4. 研究问题

### RQ1：背景 EEG 中基于运动传导路径的 Wiener 分解，能否有效降低组内虚假相干？

该问题由 V1 相干性验证回答：比较分解前后的导联间相干度，特别是 G1-G6 覆盖的运动伪迹传导路径。目标不是让所有导联完全去相关，而是降低被预定义为伪迹传导路径的共享成分。

### RQ2：不同 Wiener 约束如何改变“伪迹去除力度”和“神经信号保真度”的权衡？

比较 frequency、phase-gated、zerophase 和 scalar 条件：

- frequency 是否最强地回归组内共享方差？
- zerophase 是否保留更多具有相位延迟的颞区神经信号？
- phase-gated 是否能在两者之间提供可调折中？
- scalar 是否因为忽略频率相关传导而成为较弱基线？

### RQ3：导联分组策略是否影响预处理效果？

当前 Wiener 滤波器不按简单双侧同名导联分组，而按头颈运动伪迹的解剖传导路径分组。需要通过通道组消融实验回答：

- frontal-only、frontal+temporal、frontal+temporal+occipital、default G1-G6、all bilateral pairs 等分组策略，是否导致不同的分类和 SHAP 模式？
- G3/G6 三导联链是否比二导联对提供更多关于传导梯度的约束？
- 重叠通道的处理方式是否会影响物理解释？

### RQ4：XGBoost/SHAP 是否显示模型注意力从伪迹导联迁移到更具生理意义的背景 EEG 区域？

该问题不只看 test AUROC，还看 SHAP：

- raw 条件下模型是否过度依赖 FP1/FP2 等额极伪迹导联？
- Wiener 条件是否削弱额极伪迹导联的重要性？
- 预处理后，枕区 alpha、颞区 theta/delta 边界、Hjorth 背景组织性等特征是否更突出？
- passthrough 通道的 SHAP 变化是否只是模型共线性重分配，而不是信号本身改变？

### RQ5：当前 211 维特征空间是否限制了预处理优势向分类性能转化？

当前 `extract_epoch_features()` 只输出 211 维特征：

- 19 通道 × 9 个逐通道统计特征：delta/theta/alpha/beta/gamma 相对功率、Hjorth activity/mobility/complexity、谱熵。
- 8 对半球对称导联 × 5 个频带不对称性特征。

这些特征很少直接描述通道间相干性和相位关系，而 Wiener 预处理的核心作用恰恰发生在通道间关系上。因此需要明确评估：如果下游特征空间“不看相干性”，预处理质量的提升是否会被分类指标低估？

### RQ6：预处理算法的物理解释能否通过独立验证实验支撑，而不仅依赖分类结果？

该问题由 V1/V2/V3 组成：

- V1：分解前后相干性变化。
- V2：三导联链中的传递性约束。
- V3：滤波器幅度随频率变化的程度。

近期默认实验尚未归档完整 verification 结果，因此这些验证应作为后续实验的关键补强，而不能在论文中被提前写成既定结论。

---

## 5. 数据集与基础预处理

### 5.1 数据来源

本研究使用 TUEP v3.1.0。当前默认配置处理 AR 参考方案下的 EDF 文件：

```text
data_root/
├── 00_epilepsy/
└── 01_no_epilepsy/
```

标签编码沿用数据目录：

| 目录 | 标签 |
|---|---:|
| `00_epilepsy` | 0 |
| `01_no_epilepsy` | 1 |

### 5.2 参考方案

当前主实验使用单一参考方案以减少参考差异对模型的混淆：

| 配置键 | 当前值 |
|---|---|
| `dataset.reference_scheme` | `ar` |
| `dataset.montage_dir` | `01_tcp_ar` |

`reference_scheme` 与 `montage_dir` 必须成对变化。当前研究不再把混合 AR+LE 鲁棒性作为主实验内容。

### 5.3 EDF 读取与基础预处理

基础预处理流程如下：

```text
EDF
  -> 导联名标准化
  -> 选择标准 19 导联
  -> 连续信号 0.5-40 Hz 带通滤波
  -> 重采样到 125 Hz
  -> V 转换为 uV
  -> 排除发作前后 30 秒缓冲区
  -> 切分 20 秒非重叠 epoch
  -> 剔除幅值超过 200 uV 的 epoch
  -> cache/epochs/
```

当前默认 epoch 长度为 20 秒，因此每个 epoch 为：

$$125 \text{ Hz} \times 20 \text{ s} = 2500 \text{ samples}$$

### 5.4 数据划分

数据按 subject 级别划分，避免同一受试者的不同 epoch 同时出现在训练集和测试集中。默认比例为：

| split | 比例 |
|---|---:|
| train | 0.70 |
| val | 0.10 |
| test | 0.20 |

随机种子为 42，且按类别分层划分。

---

## 6. 核心方法：物理约束 Wiener 预处理方法族

### 6.1 导联分组：运动伪迹传导路径

Wiener 滤波只作用于预定义的头颈运动伪迹传导路径。未覆盖的中央/顶区/中线导联保持原样，作为 passthrough 通道。

| 组 | 导联 | 物理解释 |
|---|---|---|
| G1 | FP1, FP2 | 对称面部/额肌/眼动相关分量 |
| G2 | F7, T3 | 左侧胸锁乳突肌传导路径 |
| G3 | T3, T5, O1 | 左后颈部三导联链 |
| G4 | O1, O2 | 双侧枕部同步分量 |
| G5 | F8, T4 | 右侧胸锁乳突肌传导路径 |
| G6 | T4, T6, O2 | 右后颈部三导联链 |
| passthrough | F3, F4, C3, C4, P3, P4, Fz, Cz, Pz | 当前不建模其肌源性传导路径 |

这种分组的研究意义在于：它把预处理算法约束在具有解剖先验的局部路径内，而不是对全 19 通道做无差别空间滤波。

### 6.2 互功率谱估计

对每个通道组，使用 Welch 方法估计交叉功率谱矩阵：

$$S_{ij}(f) = \mathbb{E}[X_i(f)X_j^*(f)]$$

当前默认 `wiener.nperseg=500`，在 125 Hz 下对应 4 秒窗口，频率分辨率为 0.25 Hz。窗口使用 boxcar，以降低估计与后续 rFFT 应用之间的窗口不匹配。

### 6.3 相干度门控

若一个通道组在目标频带 `[0.5, 40.0]` Hz 内的最大成对相干度低于阈值，则跳过该组：

$$C_{\max} = \max_{i \ne j}\max_{f \in [0.5,40]} \gamma^2_{ij}(f)$$

若：

$$C_{\max} < 0.15$$

则不对该组做 Wiener 分解。这避免在无明显共享成分时对噪声拟合滤波器。

### 6.4 Frequency Wiener：无约束复数逐频率滤波

对目标通道 $i$，其余通道作为参考集合，逐频率求解：

$$\mathbf{h}_i(f) = \mathbf{S}_{-i,-i}^{-1}(f)\mathbf{s}_{i,-i}(f)$$

其中 $\mathbf{S}_{-i,-i}$ 是参考通道交叉谱矩阵，$\mathbf{s}_{i,-i}$ 是目标通道与参考通道之间的交叉谱向量。

该模式的优点是去除能力强，可以建模任意相位关系的线性共享成分；风险是可能把有相位延迟的真实神经耦合也作为可预测成分回归掉。

### 6.5 Scalar Wiener：固定标量消融

Scalar 模式先估计 frequency Wiener 滤波器，再在目标频带上对 $h(f)$ 取平均，得到每个参考通道一个固定标量：

$$\bar{h}_{ij} = \frac{1}{|F|}\sum_{f\in F}h_{ij}(f)$$

该模式用于检验频率相关建模的必要性。如果滤波器幅值和相位随频率变化明显，则固定标量应不足以描述宽带 EEG 中的传导关系。

### 6.6 Zerophase Wiener：实数约束与零相位假设

Zerophase 模式约束滤波器为实数，求解：

$$\Re(\mathbf{S}_{-i,-i}(f))\mathbf{h}_i(f) = \Re(\mathbf{s}_{i,-i}(f))$$

其物理动机是：EEG 频段内的组织电导近似准静态，真正的容积传导和肌源性共享成分应更接近零相位；具有非零相位差的共享分量可能包含真实神经传播。

因此，zerophase 是更保守的预处理方式：它去除的共享方差不应多于 frequency 模式，但可能保留更多真实神经信号，也可能残留更多伪迹。

### 6.7 Phase-gated Wiener：可调相位门控

Phase-gated 模式先估计标准复数 Wiener 滤波器，再根据目标-参考交叉谱的相位距离进行硬门控：

```text
若 |angle(S_target,ref(f))| <= threshold，则保留该频点滤波系数
否则该频点滤波系数置零
```

当前默认阈值为 `0.392` rad，约 22.5 度。阈值为 `pi` 时，phase-gated 等价于普通 frequency Wiener。该模式为 frequency 与 zerophase 之间提供一个可调的折中轴。

### 6.8 稳定性约束

滤波器求解使用 Tikhonov 对角加载以稳定近奇异矩阵。若任一组的滤波器幅值超过 `filter_magnitude_threshold=50.0`，该组被视为不稳定并跳过。

---

## 7. 对照方法与消融条件

### 7.1 Raw 基线

Raw 条件只经过基础 EDF 读取、带通、重采样、epoch 切分和伪迹幅值剔除，不做额外去伪迹分解。它用于衡量下游模型在未做物理约束预处理时的表现。

Raw 的高分类性能不能直接解释为神经信号更强，因为模型可能利用额肌、眼动、觉醒程度、药物副作用等非目标差异。

### 7.2 ICA 对照

ICA 条件使用 MNE FastICA，对全部 19 通道进行盲源分离，并以 FP1/FP2 均值作为额部伪迹代理。与该代理相关系数绝对值超过阈值的独立成分被剔除。

ICA 的对照意义在于：

- 它是通用盲源分离方法，而非基于运动传导路径的局部物理模型。
- 它作用于全部通道，可能改变中央/顶区/中线通道。
- 它依赖 FP1/FP2 代理，存在伪迹与额区真实神经活动纠缠的风险。

### 7.3 Wiener 方法族

当前主要比较条件为：

| 条件名 | 缓存目录 | 说明 |
|---|---|---|
| raw | `cache/epochs/` | 原始背景 epoch |
| ica | `cache/ica/` | ICA 清理后的 `specific` |
| wiener | `cache/wiener_frequency/` | 无约束复数逐频率 Wiener |
| wiener_phasegated | `cache/wiener_phasegated/` | 复数 Wiener + 零参考相位门控 |
| wiener_zerophase | `cache/wiener_zerophase/` | 实数约束逐频率 Wiener + 相位门控 |
| wiener_scalar | `cache/wiener_scalar/` | 固定标量消融，主要用于算法验证 |

XGBoost 当前支持前五个条件；scalar 主要用于分解层面的消融与验证。

---

## 8. 实验设计与评估协议

### 8.1 主实验：预处理条件比较

主实验采用预处理条件横向比较：

| 条件 | 目的 |
|---|---|
| raw | 未分解基线，观察模型是否依赖伪迹 |
| ica | 通用盲源分离对照 |
| wiener | 强去除的复数逐频率 Wiener |
| wiener_phasegated | 相位门控折中方案 |
| wiener_zerophase | 更保守的零相位方案 |

下游统一使用 XGBoost，以减少模型变量对预处理比较的干扰。

### 8.2 特征提取

当前主实验使用 211 维稳定特征向量：

| 范围 | 维度 | 内容 |
|---|---:|---|
| 0-170 | 171 | 19 通道 × 9 个逐通道特征 |
| 171-210 | 40 | 8 对半球对称导联 × 5 个频带不对称性 |

逐通道 9 个特征包括：

- delta/theta/alpha/beta/gamma 相对功率
- Hjorth activity/mobility/complexity
- 谱熵

需要明确的是，`wavelet`、`connectivity`、`complexity`、`temporal_stats` 等模块虽存在于代码库并有测试，但当前没有接入主特征提取路径。`ml.features` 中相关配置属于暂未使用配置。

### 8.3 XGBoost 训练与评估

XGBoost 采用两阶段训练：

1. GridSearchCV 选择超参数。
2. 使用验证集 early stopping 重新拟合最终模型。

评估在 subject 级别进行：

```text
epoch-level predict_proba
  -> 同一 subject 内取平均
  -> subject-level AUROC / macro-F1 / Accuracy
```

验证集用于确定 F1 最优分类阈值，该阈值随后应用到测试集。

### 8.4 SHAP 解释分析

SHAP 分析用于观察不同预处理条件下模型依赖的特征重心：

- 按频段/特征类型聚合：delta、theta、alpha、beta、gamma、Hjorth、谱熵、不对称性。
- 按通道聚合：FP1、FP2、T3、T5、O1 等导联的重要性变化。
- 横向比较：raw、ICA、Wiener frequency、phase-gated、zerophase。

SHAP 解释必须谨慎：passthrough 通道在 Wiener 条件下信号未变，但其 SHAP 仍可能因特征共线性和模型交互而变化。这类变化不能直接解释为该通道产生了新的生理信息。

### 8.5 物理验证实验

除分类外，必须保留独立的物理验证：

| 实验 | 指标 | 目的 |
|---|---|---|
| V1 | 分解前后 pairwise coherence | 验证相干成分是否被降低 |
| V2 | 三导联链传递性误差 | 检查单源/路径模型是否近似成立 |
| V3 | `|h(f)|` 频率变化 | 检验频率相关建模是否必要 |

当前代码中这些实验已经实现，但近期默认实验没有完整归档 verification 结果。后续论文实验应把 V1/V2/V3 和 XGBoost/SHAP 一起归档。

### 8.6 可视化与人工检查

脚本 05 生成：

- raw / Wiener / ICA 的多通道波形对比图。
- FP1/FP2 等目标通道的 PSD 对比图。
- 可选 EDF 导出，用于人工在 EEG 软件中并排检查 raw、wiener、wiener_phasegated、wiener_zerophase、ica。

这些结果用于质量控制，而不是独立统计结论。

---

## 9. 当前实验观察与解释框架

### 9.1 近期默认实验结果

已归档实验 `2026-07-07_104536_default_wiener-zerophase` 比较了 raw、ICA、Wiener frequency 和 Wiener zerophase：

| 条件 | Val AUROC | Test AUROC | Test F1 | Test Acc |
|---|---:|---:|---:|---:|
| raw | 0.542 | 0.738 | 0.725 | 0.730 |
| ica | 0.486 | 0.721 | 0.563 | 0.595 |
| wiener | 0.556 | 0.744 | 0.598 | 0.622 |
| wiener_zerophase | 0.583 | 0.653 | 0.619 | 0.622 |

这些结果不能简单解读为 raw 最好或 Wiener 最好。更合理的解读是：分类性能和预处理质量之间并非一一对应。

### 9.2 Raw 高分的风险

Raw 条件可能获得较高 AUROC，但如果 SHAP 显示模型高度依赖 FP1 等额极导联，则需要警惕模型利用了眼动、额肌、眨眼频率、觉醒状态或药物影响等伪迹相关差异。

这种模型在统计上可能有效，但在神经生理解释上不可靠。

### 9.3 Wiener frequency 的解释

Wiener frequency 的复数逐频率滤波器具有最强的共享方差回归能力。近期分析中，它显著降低了 FP1 的 SHAP 权重，并提升了 O1 等枕区特征的重要性。

一种合理解释是：强去除使模型从额部伪迹转向后部背景节律。但也必须承认，frequency 模式可能同时回归真实的同步神经活动，例如双侧枕区后头部优势节律。

### 9.4 Wiener zerophase 的解释

Zerophase 更保守，只去除更接近零相位的共享分量。近期结果中，zerophase 条件下 T3/T5 等颞区通道的 SHAP 权重更突出。

一种合理解释是：zerophase 保留了更多具有相位延迟的颞区神经信号，例如可能与颞叶背景异常相关的 theta/delta 边界活动。但它也可能保留更多非严格零相位的残余伪迹。

### 9.5 特征空间瓶颈

当前 211 维特征以逐通道统计为主，缺少直接度量通道间相干性、PLV、相位延迟和网络连接的特征。于是，Wiener 预处理最直接改变的“通道间关系”未必能被 XGBoost 特征空间充分看见。

这解释了一个重要现象：即便某种预处理在生理上更干净，其分类指标也未必明显提升。后续若要检验这一点，可以考虑在保持位置稳定的前提下，重新设计 connectivity 特征接入方案，但这应作为新的实验阶段，而不是混入当前 211 维主线。

---

## 10. 研究边界与局限性

### 10.1 通道组重叠与覆盖逻辑

当前 G1-G6 中存在重叠通道，例如 T3、O1、T4、O2 可能出现在多个组中。当前实现按通道组顺序写入 `specific`，后处理的组可能覆盖前一个组的结果。

这会影响物理解释：

- O1 的最终结果可能更多反映 G4 而非 G3。
- T4/O2 的最终结果可能更多反映 G6。
- 若后续组因相干度门控被跳过，较早组才会保留效果。

因此，后续实验需要归档每个 epoch 的 `skipped_pairs` 或设计更显式的重叠通道仲裁策略。

### 10.2 相干性不是伪迹的充分条件

真实神经活动也可能产生相干性，例如：

- 双侧枕区 alpha/PDR 的同步表达。
- 颞叶网络中的相位延迟传播。
- 广泛性背景节律组织性改变。

因此，研究不能把“降低相干性”直接等同于“提高信号质量”。必须结合相位、导联路径、频段、SHAP 迁移和独立验证实验共同判断。

### 10.3 211 维特征的限制

当前主线特征可解释、稳定、便于 SHAP 比较，但对 Wiener 预处理的核心效果不够敏感。尤其是通道间相干性和相位关系没有直接作为下游特征进入 XGBoost。

这意味着：

- 分类性能可能低估预处理带来的信号结构改善。
- SHAP 的通道/频段迁移比单一 AUROC 更重要。
- 后续若接入 connectivity 特征，需要重新定义主实验并处理特征维度稳定性问题。

### 10.4 验证集较小

当前默认实验 val 集只有 17 名受试者，测试集 37 名受试者。阈值选择、early stopping 和部分条件间差异可能受小样本波动影响。后续论文实验应增加置信区间、bootstrap 或多随机种子/多划分验证。

### 10.5 CNN 流水线暂缓

CNN/EEGNet 流水线已实现，可以读取 raw、ica、wiener 三种缓存并训练端到端模型。但近期研究没有继续围绕 CNN 产生实验更新，且该流水线尚未支持 phase-gated 和 zerophase 条件。

因此，本研究当前不以 CNN 为主要贡献，也不把 CNN 结果作为必须完成的主实验。CNN 可以作为未来方向：当预处理方法族稳定、物理验证充分后，再检验深度模型是否能从更干净的时空信号中获益。

---

## 11. 预期贡献

本研究的预期贡献包括：

1. **提出并实现一组物理约束 EEG 预处理算法**：以运动伪迹传导路径为先验，通过 Wiener 分解把导联信号拆分为相干成分与特异性成分。
2. **把预处理问题表述为伪迹去除与神经保真的权衡**：不再简单追求去除全部相干，而是比较 frequency、phase-gated、zerophase、scalar 等约束的不同物理含义。
3. **建立分类性能与生理解释并重的验证框架**：XGBoost 提供任务指标，SHAP 提供模型注意力迁移证据，V1/V2/V3 提供独立物理验证。
4. **指出当前特征空间的结构性瓶颈**：211 维逐通道特征适合稳定比较，但可能无法充分反映 Wiener 对通道间关系的改善。
5. **为背景 EEG 癫痫分类研究提供更谨慎的评价标准**：避免把 raw 高分直接解释为神经病理信号，强调伪迹驱动模型的风险。

---

## 12. 论文结构建议

```text
1. Introduction
   1.1 背景 EEG 在癫痫评估中的价值
   1.2 头皮 EEG 中伪迹、容积传导与参考共享的挑战
   1.3 为什么分类性能不足以评价预处理质量
   1.4 本研究贡献

2. Related Work
   2.1 发作间期/背景 EEG 癫痫分类
   2.2 EEG 伪迹去除与 ICA
   2.3 容积传导、相干性与功能连接解释风险
   2.4 Wiener 滤波与基于物理先验的信号分解

3. Methods
   3.1 TUEP 数据集与 subject-level 划分
   3.2 基础预处理：滤波、重采样、背景段提取、20 秒 epoch
   3.3 运动伪迹传导路径分组 G1-G6
   3.4 Frequency Wiener 分解
   3.5 Scalar、phase-gated、zerophase 消融
   3.6 ICA 与 raw 对照
   3.7 211 维特征提取与 XGBoost 验证
   3.8 SHAP 聚合与解释方法
   3.9 V1/V2/V3 物理验证实验

4. Experiments
   4.1 预处理条件比较：raw / ICA / Wiener variants
   4.2 通道组消融实验
   4.3 分类指标：subject-level AUROC/F1/Accuracy
   4.4 SHAP 注意力迁移分析
   4.5 独立物理验证结果

5. Results
   5.1 不同预处理条件的分类表现
   5.2 SHAP 通道与频段迁移
   5.3 Frequency vs zerophase vs phase-gated 的权衡
   5.4 通道组设计对结果的影响
   5.5 特征空间瓶颈分析

6. Discussion
   6.1 Raw 高分与伪迹驱动风险
   6.2 相干性降低不等于神经信号改善
   6.3 保守预处理与激进去除之间的选择
   6.4 当前实现局限：重叠通道、验证集规模、未归档 verification
   6.5 未来工作：connectivity 特征、多种子验证、CNN/EEGNet 复评

7. Conclusion
```

---

## 附录：核心公式速查

| 符号 | 含义 |
|---|---|
| $X_i(f)$ | 导联 $i$ 的频域表示 |
| $S_{ij}(f)$ | 导联 $i,j$ 的交叉功率谱 |
| $\gamma^2_{ij}(f)$ | magnitude-squared coherence |
| $\mathbf{h}_i(f)$ | 目标导联 $i$ 的 Wiener 滤波器 |
| $x_i^{\text{coherent}}(t)$ | 可由组内参考导联预测的相干成分 |
| $x_i^{\text{specific}}(t)$ | 从原信号中扣除相干成分后的特异性成分 |
| $\theta_{\text{phase}}$ | phase-gated / zerophase 的零参考相位门控阈值 |

---

*文档版本：v2.0 · 2026年7月8日*
*v2.0 变更：根据当前代码库和近期 git 历史，将研究主线修订为“物理约束 EEG 预处理算法研究”；补充 phase-gated、zerophase、211 维特征、XGBoost/SHAP 验证和 CNN 暂缓说明。*
