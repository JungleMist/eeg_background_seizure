extends: default.yaml

# TUAB v3.0.1 recording-level normal/abnormal classification.
# Keep these paths separate from TUEP so caches and results cannot mix.
paths:
  data_root: "/Users/jsu/Data/EEGdata/TUAB/v3.0.1"
  cache_dir: "cache_tuab"
  results_dir: "results_tuab"

dataset:
  active: "tuab"
