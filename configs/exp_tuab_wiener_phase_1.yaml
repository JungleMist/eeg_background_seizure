# Exp 1: frequency Wiener, coherence threshold 0.15, phase gate pi.
extends: exp_tuab_wiener_phase_base.yaml

paths:
  results_dir: "results_tuab/exp_wiener_phase/exp1"

wiener:
  mode: "frequency"
  overlap_policy: "coherence_weighted"
  coherence_threshold: 0.15
  phase_gate_threshold_rad: 3.141592653589793
