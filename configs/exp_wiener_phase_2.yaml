# exp_wiener_phase_2.yaml - threshold/phase ablation
# Exp 2: frequency Wiener, coherence threshold 0.45, phase gate pi.
extends: default.yaml

paths:
  results_dir: "results/exp_wiener_phase/exp2"

wiener:
  mode: "frequency"
  coherence_threshold: 0.45
  phase_gate_threshold_rad: 3.141592653589793
