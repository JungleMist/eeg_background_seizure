# Exp 8: phase-gated Wiener, coherence threshold 0.75, phase gate pi/10.
extends: exp_tuab_wiener_phase_base.yaml

paths:
  results_dir: "results_tuab/exp_wiener_phase/exp8"

wiener:
  mode: "phasegated"
  overlap_policy: "coherence_weighted"
  coherence_threshold: 0.75
  phase_gate_threshold_rad: 0.3141592653589793
