# exp_wiener_phase_6.yaml - threshold/phase ablation
# Exp 6: phase-gated Wiener, coherence threshold 0.15, phase gate pi/10.
extends: default.yaml

paths:
  results_dir: "results/exp_wiener_phase/exp6"

wiener:
  mode: "phasegated"
  coherence_threshold: 0.15
  phase_gate_threshold_rad: 0.3141592653589793
