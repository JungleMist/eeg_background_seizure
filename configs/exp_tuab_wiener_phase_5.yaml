# Exp 5: phase-gated Wiener, coherence threshold 0.15, phase gate pi/5.
extends: exp_tuab_wiener_phase_base.yaml

paths:
  results_dir: "results_tuab/exp_wiener_phase/exp5"

wiener:
  mode: "phasegated"
  overlap_policy: "coherence_weighted"
  coherence_threshold: 0.15
  phase_gate_threshold_rad: 0.6283185307179586
